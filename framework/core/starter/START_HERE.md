# Core Starter

1. Complete all files in `context/00_foundations/`.
2. Use `framework/prompts/00-session-bootstrap.md`.
3. Run feature lifecycle prompts (`02` through `14`).
4. Record handoffs and approvals.
5. Close only when context complete.
